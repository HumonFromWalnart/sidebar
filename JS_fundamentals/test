// 1. Declare a function name addItem . It takes an item parameter and it returns an array after adding the item.
//  addItem([1,2,3], 8)   --> [1,2,3,8]

// utga butsaadag

// function test2() {
//   return "Hello world";
// }

// let a = test2();
// console.log(a);

// function addItem(array, element) {
//     array[array.length] = element;
//     return array;
//   }

//   function removeItem(array, index) {
//     array[index] = 0;

//     let array2 = [];
//     for (let i = 0; i < array.length; i++) {
//       if (array[i] !== 0) {
//         array2 = addItem(array2, array[i]);
//       }
//     }
//     return array2;
//   }

//   const removedArray = removeItem([1, 2, 3, 4], 1);
//   // [1,3,4]
//   console.log(removedArray);

// function removeItem(array, index) {
//   for (let i = index; i < array.length; i++) {
//     array[i] = array[i + 1];
//   }
//   array.length = array.length - 1;
//   return array;
// }

// const removedArray = removeItem([1, 2, 3, 4], 1);
// // [1,3,4]
// console.log(removedArray);


// 3. Declare a function name sumOfNumbers. It takes a number parameter and it adds all the numbers in that range.
// sumOfNumbers([3,4,5]) ->   12
// sumOfNumbers([1,4,6]) ->   11